const add = document.querySelectorAll('.add')
const del = document.querySelectorAll('.del')
add[0].addEventListener('click', ()=>{
    add[0].parentElement.parentElement.nextSibling.nextSibling.childNodes[1].textContent
})